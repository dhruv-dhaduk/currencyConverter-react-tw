import { useCurrencies } from "./hooks";

function App() {

    // const [from, rates, changeBase, convertCurrency] = useCurrencies("USD");

    return (
        <div>
            Currency Converter
        </div>
    );
}

export default App;
